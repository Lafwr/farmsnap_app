<h1>My Events</h1>
<h2>Hey <%= current_user.username %> 👋</h2>
<hr>
<%= link_to "Browse All Events", events_path %>
<hr>
<%= link_to "New Event", new_event_path %>
<hr>
<% if @my_events.any? %>
  <ul>
    <% @my_events.each do |event| %>
      <li><h4><%= event.name %></h4></li>
      <%= link_to "Details", farmer_event_path(event.farmer, event) %>
      <%= link_to "Edit", edit_farmer_event_path(event.farmer, event) %>
      <hr>
    <% end %>
  </ul>
<% else %>
  <p>You have no events yet.</p>
<% end %>
<%= link_to "<< Dashboard", dashboard_path %>
